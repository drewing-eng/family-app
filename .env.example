# Utilisé par `npm run dev` (Vite). En production (Docker), c'est la variable
# d'environnement POCKETBASE_URL (voir docker-compose.yml / server.js) qui fait foi,
# injectée à l'exécution via /config.js — pas cette valeur figée au build.
VITE_POCKETBASE_URL=http://127.0.0.1:8090
